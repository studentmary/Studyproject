package by.stormnet.volodko.InOutSystem;

public class Validator {
	public static void validate(int registeredEmployee[]) {
	//	if (registeredEmployee[i] < 15) {
	//		System.out.println(registeredEmployee[i]);
	//	} else throws new Exception("Employees must be less 15");

	}

}
